public class Handle {
    Public static Void Match(){
        If(Handlerclass.isFirsttime == True) {
            system.debug('The value is True ');
            HandlerClass.isFirsttime = False;
        }   
    }
}